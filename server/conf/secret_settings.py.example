"""
Local secrets and environment-specific overrides.

This file is intentionally NOT committed as `secret_settings.py`.
Copy this file to `secret_settings.py` and adjust values as needed.

Used for:
- Local development
- API keys
- Host configuration
- Non-portable overrides
"""

# --------------------------------------------------
# Django / Evennia host configuration
# --------------------------------------------------

# Required for local web access (prevents DisallowedHost / 400 errors)
ALLOWED_HOSTS = [
    "localhost",
    "127.0.0.1",
    "[::1]",
]

# --------------------------------------------------
# LLM / AI configuration (optional)
# --------------------------------------------------

# Example environment-based API key usage:
# import os
# OPENAI_API_KEY = os.environ.get("OPENAI_API_KEY")

# Optional overrides if not using environment variables:
# OPENAI_MODEL = "gpt-4.1-mini"
# OPENAI_BASE_URL = "http://localhost:1234/v1"  # e.g. LM Studio

# --------------------------------------------------
# Debug / development toggles (optional)
# --------------------------------------------------

# Uncomment for extra verbosity during development
# DEBUG = True

# Example: extend INSTALLED_APPS locally
# INSTALLED_APPS += ["debug_toolbar"]

# --------------------------------------------------
# End of file
# --------------------------------------------------
